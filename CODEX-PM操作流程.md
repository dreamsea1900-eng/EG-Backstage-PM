# 用 Codex 做 prototype——PM 操作流程（一頁版）

> 給用 **OpenAI Codex** 的 PM。用 Claude Code 的人看 [README.md](README.md) 就好。
>
> 你只要做四件事：**丟檔 → 開 Codex → 送一句話 → 回答問題**。
> 切版、驗證、寫工作紀錄都是 AI 的事，你不用管。

---

## 一、第一次才要做（設定一次，以後跳過）

| # | 做什麼 | 怎麼確認好了 |
|---|---|---|
| 1 | 裝 [Codex CLI](https://developers.openai.com/codex) | 終端機打 `codex --version` 有版本號 |
| 2 | 裝 [Node.js](https://nodejs.org/) | 終端機打 `node -v` 有版本號（AI 的驗證腳本要用；**不用 `npm install`**） |
| 3 | 把整包 `EG站後台-PM` 放到本機 | 資料夾裡看得到 `AGENTS.md`、`input/`、`examples/` |

---

## 二、每做一頁，重複這四步

### 步驟 1｜材料全丟進 `input/`

參考圖、流程圖截圖（`.png` / `.jpg`）、規格或欄位清單（`.md`）——全丟進去。

- 幾個檔都行、種類可混放，**不用先整理成一份**
- **一次只放一頁的材料**
- 還沒定案的就寫「待定」，AI 會留 `TODO`、不會亂猜

### 步驟 2｜在這個資料夾裡開 Codex

終端機 `cd` 到 `EG站後台-PM`，執行 `codex`。進去後先設兩件事：

- 打 `/approvals` → 選**可寫入工作區**（workspace-write）——AI 才有權幫你建檔案
- 打 `/model` → 選**最高推理檔**——模型能力直接決定產出深度，這裡別省

> 版本較舊、沒有這兩個指令的話，改用 `codex --sandbox workspace-write` 啟動。

### 步驟 3｜送出一句話：`開始`

打「開始」按 Enter。接著 AI 會自己做完這三件事：

1. 讀完 `input/` 裡**所有**檔案
2. **回述它讀到的理解**給你看 → 有誤讀或漏檔，直接跟它說
3. **問它不清楚的地方**（每批最多 5 題，會問到清楚為止）

### 步驟 4｜回答問題（Codex 用「編號」回）

Codex 沒有點選介面，題目會長這樣：

```
② 這頁的骨架是哪種？
   1. 列表頁（我的預設猜測）
   2. 表單頁
   3. 詳情頁
```

→ 你只要回 **`1`**。多題就寫「第一題 2、第二題 1」。**不確定就回「用你的預設」。**

### 最後一關｜點頭，它才會開始做

問完後，AI 會給你一份**規格確認摘要**（最終規格、它自己猜的部分、會留的 TODO、預估花多少 token），
並問你「執行／取消」：

- 沒問題 → 回 `1`（執行）→ 它開始切版、跑驗證、給交付摘要
- 要修正 → 直接講哪裡不對 → 它改完會重新給你一份確認摘要

---

## 三、做完怎麼收貨

成果是一個資料夾，例如 `MemberDetail/`（`index.html` ＋ `css/` ＋ `js/` ＋ `WORKLOG.md`）。
**用瀏覽器打開 `index.html`** 就看得到。

- **要改**：說「**修改 MemberDetail**」＋你的意見（一次列很多條也行），它會逐條改
- **滿意了**：把頁面資料夾交回設計師 review（開 PR 或直接傳檔案都可以）
- **換下一頁**：清空 `input/`、放新材料、再走一次四步
  （清 `input/` **不會**影響已經做好的頁面）

---

## 四、卡住了看這裡

| 狀況 | 怎麼辦 |
|---|---|
| 送了「開始」但它沒讀 `input/` | 回一句：「讀 `input/` 裡的檔案，依 `AGENTS.md` 的開工流程做」 |
| 它沒問問題就直接開做 | 打斷它：「先回述你的理解，並給我規格確認摘要」 |
| 它說找不到 `node` | 裝好 Node.js，重開終端機再試 |
| 這頁想整個重來 | 把那個頁面資料夾刪掉 → 重放 `input/` → 再說「開始」 |
| 它問的東西你也還沒定 | 回「待定」，它會留 `TODO` |

---

## 五、只要記三件事

1. **不用學指令**——講人話就好；唯一要記的是改稿時說「修改 <頁面名>」
2. **不確定不要硬填**——寫「待定」比亂猜好
3. **第一版不完美很正常**——補 1～2 輪修改就到位

> 想知道更多：[README.md](README.md)（整體說明）、[input/README.md](input/README.md)（怎麼準備材料）。
